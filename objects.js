var playlist = {artistname:'song'}
function updatePlaylist(playlist, artistName, songTitle) {return playlist[artistName] = 'songTitle';}
function removeFromPlaylist(playlist, artistName) {return delete

delete the key-value pair from the playlist and return the updated playlist.